# Study PDF Viewer

공부에 최적화된 심플한 데스크톱 PDF 뷰어

## 주요 기능

- 📄 **PDF 뷰어**: 빠르고 부드러운 PDF 렌더링
- ✨ **하이라이트**: 5가지 색상의 하이라이트 지원, 줌 레벨에 따라 위치 정확히 유지
- 📝 **메모**: 하이라이트에 메모 추가 및 호버 시 툴팁으로 확인
- 🔖 **책갈피**: 여러 페이지 동시 책갈피 관리
- 🔀 **PDF 병합/분할**: 여러 PDF 합치기 및 페이지 범위 분할
- 🎨 **다크 테마**: 눈의 피로를 줄이는 다크 모드
- 💾 **자동 저장**: 모든 하이라이트/메모/책갈피 자동 저장
- 🔒 **완전 오프라인**: 인터넷 연결 불필요

## 시스템 요구사항

- Windows 10 이상
- macOS Monterey 이상
- Ubuntu 20.04 이상

## 설치 방법

### 개발 환경

1. 저장소 클론 또는 다운로드
```bash
git clone <repository-url>
cd PDFTool
```

2. 의존성 설치
```bash
npm install
```

3. 앱 실행
```bash
npm start
```

### 배포 버전 빌드

Windows:
```bash
npm run build:win
```

macOS:
```bash
npm run build:mac
```

Linux:
```bash
npm run build:linux
```

빌드된 파일은 `dist` 폴더에 생성됩니다.

## 사용 방법

### PDF 파일 열기
1. 앱 실행 후 "PDF 파일 열기" 버튼 클릭
2. 또는 PDF 파일을 드래그 앤 드롭
3. 또는 Ctrl+O (Cmd+O) 단축키 사용
4. 최근 열었던 파일 목록에서 바로 선택 가능

### 하이라이트 추가
1. 툴바에서 "하이라이트" 버튼 클릭
2. 원하는 색상 선택
3. 마우스로 하이라이트할 영역 드래그
4. 메모 입력 후 저장 (선택사항)

### 하이라이트 삭제
- 하이라이트를 우클릭 → 삭제 확인 다이얼로그에서 확인

### 책갈피 추가/제거
- 툴바의 ★ 버튼 클릭
- 또는 'B' 키 누르기

### 페이지 이동
- 이전/다음 버튼 사용
- 또는 방향키 (←, →) 사용
- 또는 페이지 번호 직접 입력
- 또는 사이드바의 썸네일 클릭

### 확대/축소
- +/- 버튼 사용
- 또는 Ctrl/Cmd + +/- 키
- 또는 Ctrl + 마우스 휠

### PDF 병합
1. 시작 화면에서 "PDF 병합" 버튼 클릭
2. 병합할 파일 2개 이상 선택
3. 저장 위치 지정

### PDF 분할
1. 시작 화면에서 "PDF 분할" 버튼 클릭
2. 분할할 PDF 선택 후 시작/끝 페이지 지정
3. 저장 위치 지정

## 단축키

| 기능 | 단축키 |
|------|--------|
| PDF 열기 | Ctrl+O (Cmd+O) |
| 확대 | Ctrl++ (Cmd++) |
| 축소 | Ctrl+- (Cmd+-) |
| 마우스 휠 확대/축소 | Ctrl + 스크롤 |
| 이전 페이지 | ← |
| 다음 페이지 | → |
| 책갈피 토글 | B |
| 종료 | Ctrl+Q (Cmd+Q) |

> 메모 입력 중에는 방향키가 페이지 이동이 아닌 커서 이동으로 동작합니다.

## 데이터 저장 위치

### Windows
```
%APPDATA%/study-pdf-viewer/
├── highlights/
│   └── [pdf-filename].json
├── bookmarks/
│   └── [pdf-filename].json
└── settings.json
```

### macOS
```
~/Library/Application Support/study-pdf-viewer/
```

### Linux
```
~/.config/study-pdf-viewer/
```

## 기술 스택

- **Electron** 27.x - 크로스 플랫폼 데스크톱 앱 프레임워크
- **PDF.js** 3.11.174 - PDF 렌더링 라이브러리
- **PDF-lib** 1.17.1 - PDF 조작 라이브러리

## 프로젝트 구조

```
PDFTool/
├── package.json          # 프로젝트 설정
├── main.js              # Electron 메인 프로세스
├── index.html           # 랜딩 페이지
├── viewer.html          # PDF 뷰어 UI
├── viewer.js            # PDF 뷰어 로직
├── assets/              # 아이콘 및 리소스
├── build/               # 빌드 설정
└── dist/                # 빌드 결과물
```

## 변경 이력

### v1.1.0
- 하이라이트 좌표를 비율값으로 저장해 줌 변경 시 위치 정확도 개선
- Ctrl+휠 확대/축소 추가
- 시작 확대율 100%로 고정
- 시작 화면 제목 잘림 현상 수정
- 메모 모달 열린 상태에서 키보드 단축키 오동작 수정
- 하이라이트 삭제 후 재생성 시 메모 입력 불가 현상 수정 (네이티브 confirm 대신 커스텀 모달 사용)
- 페이지 입력란 너비 확대 및 숫자 스피너 제거
- 최근 파일 목록 스크롤 지원
- PDF 병합/분할 기능 추가

### v1.0.0
- 최초 릴리즈

## 라이선스

MIT

## 기여

이슈 및 풀 리퀘스트는 언제나 환영합니다!

## 문의

문제가 발생하거나 질문이 있으시면 이슈를 등록해주세요.
